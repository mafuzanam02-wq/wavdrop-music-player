package com.launchpoint.wavdrop.data.settings

enum class StartupDestination(val displayName: String) {
    HOME("Home"),
    LIBRARY("Library"),
    SONGS("All Songs"),
    NOW_PLAYING("Now Playing"),
    SETTINGS("Settings"),
}
